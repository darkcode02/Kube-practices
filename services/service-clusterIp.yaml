# Ejemplo: Service ClusterIP para acceso interno dentro del cluster.
# El Deployment crea tres Pods nginx y el Service reparte tráfico entre ellos.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-2-deployment
  labels:
    app: service-2
spec:
  replicas: 3
  selector:
    # Debe coincidir con las labels del template.
    matchLabels:
      app: service-2
  template:
    metadata:
      labels:
        app: service-2
    spec:
      containers:
      - name: service-2-container
        image: nginx:latest
        ports:
        - containerPort: 80
      
---
apiVersion: v1
kind: Service
metadata:
  name: service-2-service
  labels:
    app: service-2
spec:
  # ClusterIP no expone el servicio fuera del cluster.
  type: ClusterIP
  selector:
    app: service-2
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80
