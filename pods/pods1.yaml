# Ejemplo: crear un Pod nginx desde un manifiesto sencillo.
# Es el recurso mínimo para practicar kubectl apply, get, describe y delete.
apiVersion: v1
kind: Pod
metadata:
  name: web-test
  labels:
    app: app-nginx
spec:
  containers:
    - name: app-nginx
      image: nginx:latest
