# Ejemplo: separación lógica de ambientes usando namespaces.
# Crea development y production para observar cómo los mismos nombres pueden aislarse.
apiVersion: v1
kind: Namespace
metadata:
  name: development
  labels:
    name: development
---
apiVersion: v1
kind: Namespace
metadata:
  name: production
  labels:
    name: production
---
# Carga de ejemplo en development.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: development
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: nginx:1.25-alpine
          ports:
            - containerPort: 80
---
# Carga de ejemplo en production.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ubuntu-deployment
  namespace: production
  labels:
    app: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: ubuntu:latest
          # Mantiene el contenedor vivo para poder inspeccionarlo con kubectl exec/logs.
          command: ["sleep", "3600"]
          ports:
            - containerPort: 80
